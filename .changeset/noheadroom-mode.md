---
"@raquezha/noheadroom": minor
---

Refactor noheadroom output configuration to use a new `mode` option (`normal`, `quiet`, `silent`) instead of the boolean `silent` flag, and update `PI_HEADROOM_MODE` environment variable handling.
