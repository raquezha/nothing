---
"@raquezha/norpiv": patch
---

Move the RPIV workflow package under `packages/workflows/norpiv` and add generic `.workflow/active.json` compatibility while preserving legacy `.workflow/active_task.json` behavior.
